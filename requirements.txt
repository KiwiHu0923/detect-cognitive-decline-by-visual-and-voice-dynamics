# ASR (task-region segmentation only — not a feature extractor)
mlx-whisper

# Speech features
praat-parselmouth        # phonation (jitter/shimmer/HNR/F0) + DDK intensity envelope
scipy                    # DDK peak picking, signal utilities

# Facial features
py-feat                  # AUs, emotion, head pose, gaze
mediapipe                # FaceMesh landmarks for UFNet-compatible geometric signals (eye-open, mouth-width, jaw-open, etc.)

# ML / Eval
scikit-learn
numpy
pandas
matplotlib               # ROC curves + coefficient plots
imbalanced-learn         # SMOTE for smile classifier training (matches UFNet training recipe)

# LLM
anthropic

# UI
gradio
reportlab                # PDF report export (demo/report_pdf.py)

# Utils
python-dotenv
pyyaml
tqdm
openpyxl                 # read IPVS FILE CODES.xlsx and metadata sheets
